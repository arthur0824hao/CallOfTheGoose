[pytest]
asyncio_mode = auto
addopts = 
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
